# SenseStream poller: small FastAPI service + Postgres writer.
# Pulls numpy in too because in stub mode the poller imports the
# synthetic generator from scripts/.

fastapi==0.135.3
uvicorn==0.44.0
starlette==1.0.0
anyio==4.13.0

httpx==0.28.1
psycopg[binary]==3.3.3

# synthetic generator dependency
numpy==2.4.3
